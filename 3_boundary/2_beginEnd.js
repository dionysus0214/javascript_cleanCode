// begin, end: 경계를 포함하지만 제외하는 경우
function reservationDate(beginDate, endDate) {
  // ...
}
reservationDate('YYYY-MM-DD', 'YYYY-MM-DD');

// first, last: 포함된 양 끝을 의미(부터 ~까지)
const students = ['poco', 'john', 'tom'];
function getStudents(fitst, last) {
  //
}
getStudents('poco', 'tom');